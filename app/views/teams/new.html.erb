<%= form_for @team do |f|%>
	<%= f.label :name%>
	<%= f.text_field :name %>
	<%= f.label :description %>
	<%= f.text_field :description %>
	<%= f.hidden_field :company_id, value: params[:company_id]%>
	<%= f.submit "Create Team" %>
<% end %>
